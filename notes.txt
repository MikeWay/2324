DONE: CN: Need a quick mention of SASS / SCSS early on 
DONE: Module stuff has changed. All the notes around the bootstrap have to changed

DONE CN: Remove Module stuff from C2 -- shift to C6
DONE CN: Need to address standalone and imports prior to EX2.1

DONE: EX 2.2 Need to use new for / if

DONE CN: Pre ex2.2 must show both @for and @if template constructs (Known as @for block and @if block)
CN: [ngClass] -- need to import NgClass for the binding to work

CN: Router changes (see Ex5.1) due to non using a module. Loaded in app.config

EX5.2
DONE 1) Convert AccountsComponent to not standalone - before Step 3
DONE 2) Need to look at the whole module load thing!
DONE 3) Seems to work - needs tidy. Look at deffered component load for the 2nd bonus

DONE CN: Prior to 5.2 we need to discuss non-standalone components
DONE CN: Explain difference with dependencis when using modules
DONE CN: Some of the stuff from earlier in the course ref modules will be needed here

CN: Somewhere we need to make sure that the ApplicationStateService is in a diagram to give some context. (C3 ?)

NEXT Finish Testing 5.2 Bonus_1 (the bit about @defer)

NO! CN: Need to discuss overrideComponent() for testing standalone components prior to DN61

REMOVED: CN and MA: Need to consider how usefull the DN62 actually is

DONE CN: Prior to ex6.1 must show diff of test module for standalone vs module based.
DONE CN: Prior to ex6.1 discuss Partials

MA: Need to rationalise file paths across entire MA
DONE CN: C7 Improve discussion of Template vs Reactive forms

CN: C7 -- Architecture ref ex7.1

DONE CN: C8 - explain provision of HttpClient in app Config
CN:C8 Order of provideHttpClient(),provideHttpClientTesting() is critical

**** Issue with Cypress vs Jasmine in VSCode

Need to do this:
Add   "exclude": [
    "cypress/**/*.ts",
    "cypress.config.ts"
  ]
  to tsconfig.json

  Add this to cypress/tsconfig.json:
  "include": [
    "**/*.ts",
    "../cypress.config.ts"
  ],
  "exclude": []


NO! CN/EX8.2 Note that the websocket subscribe does not work with the new format for subscribe. (May be in notes already)

For Material: need to make sure the slides show the code we need incluing:
GridList
Card
CDK Layout - Breakpoint

C9: Bundle sizes: analyzer: https://esbuild.github.io/analyze/