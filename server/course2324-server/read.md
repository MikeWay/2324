REST Server to support course 2324
==================================

Build using mvn package -- from the COMMAND LINE, not Eclipse.
Deploy by copying the WAR file into the hot-deploy directory of Tomcat

Expects an Airline database as defined by resources/application.properties